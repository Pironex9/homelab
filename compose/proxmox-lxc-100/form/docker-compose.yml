services:
  form:
    image: caddy:alpine
    container_name: form
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - ./html:/usr/share/caddy
    ports:
      - 3004:80
    restart: unless-stopped
